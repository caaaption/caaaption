import SwiftUI

public struct HashtagLabel: View {
  public var body: some View {
    Text("")
  }
}

struct HashtagLabelPreview: PreviewProvider {
  static var previews: some View {
    Group {
      HashtagLabel()
    }
  }
}
